{
  "name": "nivasa-web",
  "version": "1.0.0",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "client": "cd frontend && npm run dev",
    "server": "cd backend && npm run start-full",
    "dev": "concurrently \"npm run client\" \"npm run server\""
  },
  "dependencies": {
    "date-fns": "^4.1.0"
  }
}
